---
name: agent-worklog
description: >-
  扫描本机现存的命令行 AI Agent（Claude Code、Codex、Grok）对话记录，抽取真实工作意图，
  按 日/周/月 汇总成一张工作记录表。触发词：工作记录、工作复盘、工作总结、日报、周报、月报、
  我这周做了什么、总结我的工作、agent worklog、work log、daily/weekly/monthly work report。
  用于把散落在多个 AI 工具里的对话，还原成一份可读、可分享的工作台账。可移植、可分享给他人。
---

# agent-worklog · 跨-Agent 工作记录生成器

把你散落在 **Claude Code / Codex / Grok** 里的现存对话记录扫出来、去噪、按时间汇总成一张
**工作记录表**。回答的是「我（这天/这周/这月）到底做了什么」。

- **采集器只在本地运行**：脚本只读本地日志、不联网、不改原始文件；如果后续交给云端 Agent 总结，原料会按该服务的隐私政策发送给模型提供商。
- **默认脱敏**：输出前尝试遮盖常见密钥、邮箱和用户目录；仍需人工检查项目名、客户名和内部信息。
- **可移植**：自动探测家目录下的日志位置 + 自动用系统本地时区，换台机器/换个人也能跑。
- **可分享**：整个 skill 目录自包含，拷给别人即可用（见 `README.md`）。

## 支持的数据源

| Agent | 日志位置 | 状态 |
|---|---|---|
| Claude Code | `~/.claude/projects/*/*.jsonl` | ✅ 已支持 |
| Codex | `~/.codex/sessions/**` + `archived_sessions/**` | ✅ 已支持 |
| Grok | `~/.grok/sessions/*/*/chat_history.jsonl` | ✅ 已支持 |
| Gemini / Cursor / Qwen / Continue / Aider | 各自目录 | 🔍 仅检测并报告（格式未支持，见 README 扩展说明）|

## Workflow

激活本 skill 后，严格按以下步骤执行：

### 1. 确认粒度与窗口
从用户请求判断时间粒度，缺省则询问一次：
- **日**（day）：默认近 7 天 —— 「日报」「今天/这几天做了什么」
- **周**（week）：默认近 28 天（4 周）—— 「周报」「这周/上周」
- **月**（month）：默认近 90 天（3 个月）—— 「月报」「这个月/这季度」

用户可指定窗口（如「近 14 天」「最近两个月」），换算成 `--since <天数>`。

### 2. 运行采集器（脚本负责扫描+去噪+分桶，不负责总结）
```bash
python "<skill目录>/scripts/collect_agent_logs.py" --granularity <day|week|month> --since <天数> --out "<输出目录>"
```
可选参数：
- `--project <关键词>`：用户只想看某个项目时加上（如「只看 Philthink 的周报」→ `--project philthink`）。
- `--csv`：用户要 Excel/飞书表格时加上，额外导出 `worklog-summary.<粒度>.csv`。
- 默认开启脱敏；只有用户明确需要原始内容并理解风险时才加 `--no-redact`。
- Windows 若中文报错，前面加 `PYTHONUTF8=1`（PowerShell：`$env:PYTHONUTF8=1;`）。
- 输出：`<输出目录>/worklog-intents.<粒度>.md`（可读原料）+ 同名 `.json`。
- 终端会打印 **Agent 探测报告**（各源会话数 + 检测到但未支持的 Agent）——把它如实转述给用户。

### 3. 读取原料
读 `worklog-intents.<粒度>.md`。每个时间分组下，是按 Agent 分类、过滤系统噪音并默认脱敏后的**真实用户意图**。

如果当前 Agent 是云端服务，先提醒用户：读取原料会把内容发送给该服务。用户要求纯本地处理时，不要把原料交给云端模型。

### 4. 逐期总结成表（这一步由你/Agent 完成，脚本做不了）
对每个时间分组，把该期的意图**归纳成 2–4 条具体工作**（不要罗列原句，要提炼主线：做了什么、推进到哪、涉及哪个项目）。
按粒度选列：

- **日**：`日期 | 主要工作 | 涉及项目 | 主力工具 | 会话数`
- **周**：`周次 | 本周主线/进展 | 重点项目 | 主力工具`
- **月**：`月份 | 主要成果/主线 | 重点项目`

先出**总览表**，再出**逐期明细**（每期 2–4 条 bullet）。摘要要客观陈述事实，不写内部策略话术。

### 5. 输出与交付
- 写到 `<输出目录>/工作记录-<粒度>-<生成日期>.md`；若用户的项目里有约定的工作日志目录（如 Obsidian vault 的 `工作日志/`），优先放那里。
- 用 Markdown（frontmatter + 表格）；在 Obsidian vault 内则遵循 Obsidian Flavored Markdown。
- 结尾附「数据来源与局限」：覆盖了哪些 Agent、哪些检测到但未纳入、会话数≠工作量、网页/App 版聊天本机无日志需手动补。

### 6. 分享提醒（当用户说要把结果发给别人时）
- **代码可分享**：skill 本身不含个人数据，随便拷。
- **报告需先审阅**：生成的工作记录含真实项目名、文件路径、业务信息——发出去前提醒用户自己过一遍，删敏感项。
- 默认脱敏只是第一道防线，不能替代人工审阅。

## 关键约束
- 逐条日期用系统本地时区过滤；不要自己再假设时区。
- 每期每源默认保留最新 60 条去重意图；需要更多原料时提高 `--max-intents`。
- Claude 的「轮次」含工具返回会偏高，表里用**会话数**，不要把轮次当工作量。
- 找不到某个 Agent 是正常的（用户没装），跳过并说明，不要报错或编造。
- 只做汇总，不改动任何 Agent 的原始日志。

## Read
- 首次使用或需要扩展新 Agent、排查路径时，读 `README.md`。
